# GUI dependencies (pinned to the versions the interface was tested against).
# The oq-pfdha package itself is ALSO required, because the GUI runs the
# real engine. From the repository root:
#
#     pip install -e .
#     pip install -r webgui_demo/requirements.txt
#
# Note: oq-pfdha pins its own numpy/pandas-compatible versions; install it
# FIRST and let pip resolve. The pins below are upper-level GUI libraries
# that do not conflict with the engine stack.
-e .
streamlit==1.58.0
plotly==6.8.0
pydeck==0.9.2
openquake.engine==3.25.1
