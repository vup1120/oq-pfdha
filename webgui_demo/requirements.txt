# Pinned to the versions the demo was developed and tested against.
# The app itself needs only these; the oq-pfdha package is NOT required at
# runtime (model lists come from the committed registry.json snapshot and
# results from the committed data/ CSVs). Install oq-pfdha only if you want
# to regenerate registry.json via generate_registry.py.
streamlit==1.58.0
pandas==3.0.3
numpy==2.4.6
plotly==6.8.0
pydeck==0.9.2
