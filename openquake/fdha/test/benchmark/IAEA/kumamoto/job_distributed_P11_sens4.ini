[general]
description = Kumamoto SENSITIVITY 4: four rupture sources (middle branches), site r=10 km - P11 chain (snapshot-validated)
calculation_mode = fdha_classical

[geometry]
sites = 130.7196 32.8288
# The sensitivity site sits at r = 10 km, on the default integration
# cutoff (max_distance_km = 10); widen it so the site is kept.
max_distance_km = 50.0

[site_params]
reference_vs30_value = 600.0

[erf]
rupture_mesh_spacing = 1.0
width_of_mfd_bin = 0.1

[calculation]
investigation_time = 1.0
displacement_measure_levels = {"FD": [0.0001, 0.001, 0.005, 0.01, 0.015, 0.03, 0.05, 0.075, 0.1, 0.15, 0.3, 0.5, 0.75, 1.0, 3.0, 5.0, 7.5, 10.0]}
r_threshold_km = 0.5
fdha_logic_tree_file = flt_distributed_P11.xml
source_model_logic_tree_file = smlt_sens4.xml
